<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.noodletrail.share.mapper.ShareRecordMapper">

    <resultMap id="BaseResultMap" type="com.example.noodletrail.share.entity.ShareRecord">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="target_type" property="targetType" />
        <result column="target_id" property="targetId" />
        <result column="share_channel" property="shareChannel" />
        <result column="share_title" property="shareTitle" />
        <result column="share_desc" property="shareDesc" />
        <result column="share_cover" property="shareCover" />
        <result column="share_time" property="shareTime" />
        <result column="ip" property="ip" />
    </resultMap>

    <insert id="insert" parameterType="com.example.noodletrail.share.entity.ShareRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO share_record (user_id, target_type, target_id, share_channel, share_title, share_desc, share_cover, share_time, ip)
        VALUES (#{userId}, #{targetType}, #{targetId}, #{shareChannel}, #{shareTitle}, #{shareDesc}, #{shareCover}, #{shareTime}, #{ip})
    </insert>

    <select id="countByUserAndTarget" resultType="int">
        SELECT COUNT(1)
        FROM share_record
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

</mapper>
