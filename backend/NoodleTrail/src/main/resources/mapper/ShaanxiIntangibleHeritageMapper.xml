<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.noodletrail.heritage.mapper.ShaanxiIntangibleHeritageMapper">

    <!-- 查询所有非遗信息 -->
    <select id="findAll" resultType="com.example.noodletrail.heritage.entity.ShaanxiIntangibleHeritage">
        SELECT id,
               heritage_name         AS heritageName,
               heritage_type         AS heritageType,
               image_url             AS imageUrl,
               video_url             AS videoUrl,
               historical_origin     AS historicalOrigin,
               content_introduction  AS contentIntroduction
        FROM shaanxi_intangible_heritage
        ORDER BY id ASC
    </select>

    <!-- 按非遗分类查询 -->
    <select id="findByType" resultType="com.example.noodletrail.heritage.entity.ShaanxiIntangibleHeritage">
        SELECT id,
               heritage_name         AS heritageName,
               heritage_type         AS heritageType,
               image_url             AS imageUrl,
               video_url             AS videoUrl,
               historical_origin     AS historicalOrigin,
               content_introduction  AS contentIntroduction
        FROM shaanxi_intangible_heritage
        <where>
            <if test="heritageType != null and heritageType != ''">
                heritage_type = #{heritageType}
            </if>
        </where>
        ORDER BY id ASC
    </select>

    <!-- 模糊搜索（名称 / 历史渊源 / 内容介绍） -->
    <select id="searchByKeyword" resultType="com.example.noodletrail.heritage.entity.ShaanxiIntangibleHeritage">
        SELECT id,
               heritage_name         AS heritageName,
               heritage_type         AS heritageType,
               image_url             AS imageUrl,
               video_url             AS videoUrl,
               historical_origin     AS historicalOrigin,
               content_introduction  AS contentIntroduction
        FROM shaanxi_intangible_heritage
        <where>
            <if test="keyword != null and keyword != ''">
                (
                    heritage_name LIKE CONCAT('%', #{keyword}, '%')
                    OR historical_origin LIKE CONCAT('%', #{keyword}, '%')
                    OR content_introduction LIKE CONCAT('%', #{keyword}, '%')
                )
            </if>
        </where>
        ORDER BY id ASC
    </select>

</mapper>
